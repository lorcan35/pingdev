from setuptools import setup, find_packages

setup(
    name='pingos',
    version='0.1.0',
    packages=find_packages(),
    python_requires='>=3.8',
    description='Python SDK for PingOS gateway',
)
