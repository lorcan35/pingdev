/** Gemini-specific types used by tool and mode managers. */

/** Gemini tool names (toggleable via Tools menu). */
export type GeminiTool = 'deep_research' | 'create_videos' | 'create_images' | 'canvas' | 'guided_learning' | 'deep_think';

/** Gemini mode names (via mode picker). */
export type GeminiMode = 'fast' | 'thinking' | 'pro';
