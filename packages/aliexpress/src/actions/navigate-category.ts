import type { ActionHandler } from '@pingdev/core';
import { selectors } from '../selectors.js';

/** Navigates to a specific product category page */
export const navigateCategory: ActionHandler = async (ctx) => {
};
