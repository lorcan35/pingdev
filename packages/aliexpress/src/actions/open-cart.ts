import type { ActionHandler } from '@pingdev/core';
import { selectors } from '../selectors.js';

/** Opens the shopping cart view to see selected items */
export const openCart: ActionHandler = async (ctx) => {
};
