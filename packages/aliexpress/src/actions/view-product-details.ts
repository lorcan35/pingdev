import type { ActionHandler } from '@pingdev/core';
import { selectors } from '../selectors.js';

/** Opens the detailed view of a specific product listing */
export const viewProductDetails: ActionHandler = async (ctx) => {
};
