import type { ActionHandler } from '@pingdev/core';
import { selectors } from '../selectors.js';

/** Searches for items using the main search bar in the header */
export const searchProducts: ActionHandler = async (ctx) => {
};
